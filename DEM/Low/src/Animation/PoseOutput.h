#pragma once
#include <StdDEM.h>

// Interface for writing 3D transforms to multiple targets (for example, bone nodes)

namespace DEM::Anim
{

class IPoseOutput
{
public:

	// virtual SetS/R/T(port)
	// port binding for bone map (or only in skeleton?)
};

}
